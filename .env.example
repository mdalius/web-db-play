# PostgreSQL container credentials. Copy to .env before starting the stack.
POSTGRES_DB=web_db_play
POSTGRES_USER=web_app
POSTGRES_PASSWORD=change_this_password

# PostgreSQL schema used by the application
POSTGRES_SCHEMA=public
DB_SCHEMA=public
